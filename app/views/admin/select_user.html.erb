<% form_tag(:action => "edit_user_permissions", :method => :post) do |f| %>
	<%= select_tag "user_id", options_from_collection_for_select(@users, "id", "email") %>
	<%= submit_tag "Select User"  %>
<% end %>